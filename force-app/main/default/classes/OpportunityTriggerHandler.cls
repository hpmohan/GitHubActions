public with sharing class OpportunityTriggerHandler {
    // this is a sample comment
    public OpportunityTriggerHandler() {
    
    }
    
    // autoClose comment updated
    public void autoClose(List<Opportunity> oppList){
        if(oppList.size() == 0){
            
        }
    }
}
